# probe_id: R4.3
# form: ASK
PREFIX ex: <https://example.org/ins/>
ASK {
  <{{claim_iri}}> ex:hasSubrogationCase ?case .
  ?case ex:subrogationStatusCode ?status .
  FILTER(?status IN ("OPEN", "NEGOTIATING", "DEMANDED"))
  FILTER NOT EXISTS {
    <{{claim_iri}}> ex:hasRecovery ?rec .
  }
}
